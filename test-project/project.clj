;; This project and the files contained in its folders are support for the 
;; tests in /test. Do not modify these files without also modifying the tests.

(defproject lein-multi-test "0.1.0"
  :dependencies [[org.clojure/clojure "1.2.0-RC1"]])